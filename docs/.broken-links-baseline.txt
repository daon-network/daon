# Known-broken internal links on the docs site.
#
# This file is intentionally empty.
#
# It used to hold 98 entries: links on published pages pointing at pages that
# were never written -- a DMCA generator, an evidence kit, SDK guides, a
# newsletter, a status page. The baseline let CI fail on *new* breakage without
# blocking on the backlog, which was reasonable, but it also made 98 dead
# promises a permanent fixture rather than a bug.
#
# The links have been removed. A link to a page that does not exist is a claim
# the site cannot keep, which is the same problem as the invented testimonials
# and the SDKs nobody could install.
#
# If a link genuinely must break temporarily, add it here with a reason and a
# date. An entry with neither should be deleted, not inherited.
